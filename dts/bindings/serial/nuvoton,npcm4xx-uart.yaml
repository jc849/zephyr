description: NUVOTON NPCM4XX FAMILY UART

compatible: "nuvoton,npcm4xx-uart"

include: ["uart-controller.yaml", "nuvoton-pinctrl.yaml"]

properties:
    reg:
        required: true
    clocks:
        required: true
